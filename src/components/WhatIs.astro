---
const stats = [
  { value: '+500', label: 'Miembros activos', color: '#00e5ff' },
  { value: '50+',  label: 'Herramientas IA',  color: '#8b5cf6' },
  { value: '48h',  label: 'Para lanzar',       color: '#00ffa3' },
];
---

<section
  id="que-es"
  class="section-pad"
  style="background-color: var(--color-surface); position: relative; overflow: hidden;"
>
  <!-- Subtle grid background -->
  <div
    aria-hidden="true"
    class="bg-line-grid"
    style="position: absolute; inset: 0; opacity: 0.5; pointer-events: none;"
  ></div>

  <!-- Top divider glow -->
  <div
    aria-hidden="true"
    style="position:absolute;top:0;left:50%;transform:translateX(-50%);width:60%;height:1px;background:linear-gradient(90deg,transparent,rgba(0,229,255,0.4),rgba(139,92,246,0.4),transparent);"
  ></div>

  <div class="wrap relative z-10">
    <div class="grid lg:grid-cols-2 gap-12 xl:gap-20 items-center">

      <!-- Left: text + stats -->
      <div>
        <p class="section-label">Qué es MetaDigitales</p>

        <h2 style="
          font-family: var(--font-display);
          font-weight: 800;
          font-size: clamp(2rem, 4vw, 3rem);
          letter-spacing: -0.03em;
          line-height: 1.1;
          color: var(--color-ink);
          margin-bottom: 1.375rem;
        ">
          La comunidad donde
          <span class="text-gradient"> las ideas se convierten</span>
          en ingresos.
        </h2>

        <p style="font-size: 1.0625rem; color: var(--color-muted); line-height: 1.7; max-width: 500px; margin-bottom: 2.5rem;">
          MetaDigitales es un espacio privado para emprendedores que quieren
          construir negocios digitales reales usando IA como su co-fundador.
          Aquí encontrarás templates, automatizaciones, mentoring y una
          comunidad que ya lo está haciendo — y generando resultados.
        </p>

        <!-- Stats grid -->
        <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem;">
          {stats.map(stat => (
            <div
              style={`
                padding: 1.25rem 1rem;
                border: 1px solid ${stat.color}22;
                border-radius: 10px;
                background: ${stat.color}08;
                text-align: center;
              `}
            >
              <div style={`
                font-family: var(--font-display);
                font-weight: 800;
                font-size: 1.875rem;
                letter-spacing: -0.03em;
                color: ${stat.color};
                line-height: 1;
                margin-bottom: 0.375rem;
              `}>
                {stat.value}
              </div>
              <div style="font-size: 0.75rem; color: var(--color-muted); font-family: var(--font-mono); letter-spacing: 0.04em;">
                {stat.label}
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Right: decorative card with manifesto -->
      <div
        style="
          background: linear-gradient(135deg, rgba(11,11,26,0.9) 0%, rgba(17,17,39,0.9) 100%);
          border: 1px solid rgba(139, 92, 246, 0.2);
          border-radius: 16px;
          padding: 2.25rem;
          box-shadow: 0 0 60px rgba(139,92,246,0.08), 0 24px 48px rgba(0,0,0,0.3);
          position: relative;
          overflow: hidden;
        "
      >
        <!-- Accent corner glow -->
        <div
          aria-hidden="true"
          style="
            position: absolute; top: 0; right: 0;
            width: 200px; height: 200px;
            background: radial-gradient(circle at top right, rgba(139,92,246,0.15) 0%, transparent 70%);
            pointer-events: none;
          "
        ></div>

        <!-- Card header -->
        <div style="display:flex;align-items:center;gap:0.75rem;margin-bottom:1.75rem;padding-bottom:1.25rem;border-bottom:1px solid rgba(255,255,255,0.06);">
          <div style="
            width:36px;height:36px;border-radius:8px;
            background:linear-gradient(135deg,#8b5cf6,#6d28d9);
            display:flex;align-items:center;justify-content:center;
            flex-shrink:0;
          ">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" aria-hidden="true">
              <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>
            </svg>
          </div>
          <div>
            <div style="font-family:var(--font-display);font-weight:700;font-size:0.9375rem;color:var(--color-ink);">Manifiesto MetaDigitales</div>
            <div style="font-family:var(--font-mono);font-size:0.6875rem;color:var(--color-muted);">v2.0 &nbsp;·&nbsp; 2025</div>
          </div>
        </div>

        <!-- Manifesto items -->
        <div style="display:flex;flex-direction:column;gap:1.125rem;">
          {[
            { icon: '→', text: 'Cualquier idea puede convertirse en un negocio digital que genera ingresos.', highlight: false },
            { icon: '→', text: 'La IA no reemplaza al emprendedor. Lo convierte en un equipo de uno.', highlight: true },
            { icon: '→', text: 'Automatizar no es el futuro. Es el presente de quienes ya ganan.', highlight: false },
            { icon: '→', text: 'Una comunidad que construye junta, escala junta.', highlight: false },
          ].map(item => (
            <div style="display:flex;gap:0.875rem;align-items:flex-start;">
              <span style={`
                font-family: var(--font-mono);
                font-size: 0.875rem;
                font-weight: 600;
                color: ${item.highlight ? '#00e5ff' : '#8b5cf6'};
                flex-shrink: 0;
                margin-top: 0.1em;
              `}>{item.icon}</span>
              <p style={`
                font-size: 0.9375rem;
                line-height: 1.55;
                color: ${item.highlight ? 'var(--color-ink)' : 'var(--color-muted)'};
                ${item.highlight ? 'font-weight: 500;' : ''}
              `}>{item.text}</p>
            </div>
          ))}
        </div>
      </div>

    </div>
  </div>
</section>
