---
const trustItems = [
  'Acceso inmediato',
  'Comunidad activa',
  'Cancela cuando quieras',
];
---

<section
  id="unete"
  class="section-pad"
  style="background-color: var(--color-void); position: relative; overflow: hidden;"
>
  <!-- Radial gradient background -->
  <div
    aria-hidden="true"
    style="
      position: absolute; inset: 0;
      background:
        radial-gradient(ellipse 70% 60% at 30% 50%, rgba(139,92,246,0.12) 0%, transparent 70%),
        radial-gradient(ellipse 50% 50% at 70% 50%, rgba(0,229,255,0.08) 0%, transparent 70%);
      pointer-events: none;
    "
  ></div>

  <!-- Top neon line -->
  <div
    aria-hidden="true"
    style="
      position: absolute;
      top: 0;
      left: 10%; right: 10%;
      height: 1px;
      background: linear-gradient(90deg, transparent, #8b5cf6, #00e5ff, transparent);
    "
  ></div>

  <!-- Background grid -->
  <div
    aria-hidden="true"
    class="bg-dot-grid fade-edges"
    style="position: absolute; inset: 0; pointer-events: none; opacity: 0.6;"
  ></div>

  <div class="wrap relative z-10" style="text-align: center;">

    <!-- Tag -->
    <div
      style="
        display: inline-flex;
        align-items: center;
        gap: 0.625rem;
        margin-bottom: 2rem;
        padding: 0.375rem 1rem 0.375rem 0.75rem;
        border: 1px solid rgba(0, 255, 163, 0.25);
        border-radius: 100px;
        background: rgba(0, 255, 163, 0.06);
      "
    >
      <span
        style="
          width: 7px; height: 7px;
          border-radius: 50%;
          background: #00ffa3;
          box-shadow: 0 0 8px #00ffa3;
          flex-shrink: 0;
        "
      ></span>
      <span style="font-family: var(--font-mono); font-size: 0.75rem; color: #00ffa3; letter-spacing: 0.08em;">
        Únete ahora
      </span>
    </div>

    <!-- Headline -->
    <h2 style="
      font-family: var(--font-display);
      font-weight: 800;
      font-size: clamp(2.5rem, 6vw, 4.5rem);
      letter-spacing: -0.04em;
      line-height: 1.02;
      color: var(--color-ink);
      max-width: 720px;
      margin: 0 auto 1.5rem;
    ">
      Tu negocio digital
      <span class="text-gradient"> empieza hoy.</span>
    </h2>

    <!-- Subtext -->
    <p style="
      font-size: clamp(1rem, 1.5vw, 1.1875rem);
      color: var(--color-muted);
      max-width: 560px;
      margin: 0 auto 2.75rem;
      line-height: 1.65;
    ">
      Accede a todas las herramientas, la comunidad y el conocimiento que
      necesitas para construir tu negocio automatizado con IA.
    </p>

    <!-- Main CTA -->
    <div style="display:flex;flex-direction:column;align-items:center;gap:1.75rem;">
      <a
        href="https://comunidad.metadigitales.com"
        class="btn-primary animate-pulse-glow"
        target="_blank"
        rel="noopener"
        style="font-size: 1.0625rem; padding: 1.0625rem 2.5rem;"
      >
        Unirme a MetaDigitales
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>

      <!-- Trust indicators -->
      <div style="display:flex;flex-wrap:wrap;justify-content:center;gap:1.5rem;">
        {trustItems.map(item => (
          <span style="display:flex;align-items:center;gap:0.5rem;font-size:0.875rem;color:var(--color-muted);">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#00ffa3" stroke-width="2.5" aria-hidden="true">
              <polyline points="20 6 9 17 4 12"/>
            </svg>
            {item}
          </span>
        ))}
      </div>
    </div>

    <!-- Decorative metric cards row -->
    <div
      style="
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
        margin-top: 4rem;
        padding-top: 3rem;
        border-top: 1px solid rgba(255,255,255,0.05);
      "
    >
      {[
        { label: 'Miembros activos',     value: '+500',   color: '#00e5ff' },
        { label: 'Negocios lanzados',    value: '+100',   color: '#8b5cf6' },
        { label: 'Herramientas IA',      value: '50+',    color: '#00ffa3' },
        { label: 'Tiempo hasta lanzar',  value: '48h',    color: '#00e5ff' },
      ].map(m => (
        <div
          style={`
            padding: 1.25rem 1.75rem;
            border: 1px solid ${m.color}18;
            border-radius: 10px;
            background: ${m.color}06;
            text-align: center;
            min-width: 130px;
          `}
        >
          <div style={`
            font-family: var(--font-display);
            font-weight: 800;
            font-size: 1.75rem;
            letter-spacing: -0.03em;
            color: ${m.color};
            line-height: 1;
            margin-bottom: 0.375rem;
          `}>{m.value}</div>
          <div style="font-size: 0.75rem; color: var(--color-muted); font-family: var(--font-mono);">
            {m.label}
          </div>
        </div>
      ))}
    </div>

  </div>
</section>
